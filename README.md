# Subscription Management Platform (SMP)

Subscription Management Platform (SMP) — это backend-сервис для управления подписками пользователей.
Проект реализуется как production-ready API с акцентом на бизнес-логику, чистую архитектуру и DevOps-практики
(CI/CD и Kubernetes).

---

## 🎯 Цель проекта

Целью проекта является разработка системы, которая позволяет:
- управлять тарифными планами;
- оформлять и контролировать подписки пользователей;
- реализовать пробный период (trial);
- ограничивать доступ к функционалу на основе статуса подписки;
- подготовить сервис к развертыванию в Kubernetes с использованием CI/CD.

Проект создаётся как учебно-практический, ориентированный на реальные SaaS-сценарии.

---

## 🧩 Основные понятия

### План (Plan)
План — это тарифный шаблон, который определяет условия подписки:
- название;
- стоимость;
- длительность;
- набор возможностей.

Один план может использоваться многими пользователями.

### Подписка (Subscription)
Подписка — это экземпляр плана, оформленный конкретным пользователем.
Подписка имеет:
- статус (`trial`, `active`, `expired`, `cancelled`);
- дату начала и окончания;
- связь с пользователем и планом.

У пользователя может быть **только одна активная или trial-подписка одновременно**,
но при этом сохраняется история всех прошлых подписок.

---

## ⚙️ Функциональные возможности (MVP)

### Аутентификация
- Регистрация пользователя
- Вход и выход из системы
- Cookie-based сессионная аутентификация

### Подписки
- Активация пробной подписки (trial)
- Активация платной подписки
- Отмена подписки
- Автоматическое истечение подписки по сроку
- Просмотр текущей подписки пользователя

### Планы
- Создание и управление тарифными планами (admin)
- Получение списка доступных планов

### Администрирование
- Просмотр подписок пользователей
- Фильтрация по статусам
- Базовая статистика

---

## 🏗 Архитектура

Проект построен по принципу разделения ответственности:

API (FastAPI routers)
↓
Services (бизнес-логика)
↓
Repositories (работа с БД)
↓
Database (PostgreSQL)


- В роутерах отсутствует бизнес-логика
- Все правила реализованы в сервисах
- ORM используется через SQLAlchemy 2.0

---

## 🛠 Технологический стек

- Python 3.11
- FastAPI
- SQLAlchemy 2.0 (async)
- PostgreSQL
- asyncpg
- Alembic
- Pydantic
- Ruff (lint + format)
- Docker
- GitHub Actions (CI)
- Kubernetes (Deployment, Service, ConfigMap, Secret)

---

## 🚀 Этапы реализации

1. Реализация доменных моделей (User, Plan, Subscription)
2. Бизнес-логика управления подписками
3. REST API (v1)
4. Миграции базы данных
5. Логирование и healthcheck endpoints
6. Тестирование бизнес-логики
7. Docker-контейнеризация
8. CI/CD pipeline
9. Развертывание в Kubernetes

---

## ✅ Результат

В результате реализации проекта будет получен:
- production-ready backend-сервис;
- практический опыт построения SaaS-архитектуры;
- опыт настройки CI/CD и Kubernetes;
- полноценный проект для портфолио.

---

## 📌 Статус проекта

Проект находится в активной разработке.
